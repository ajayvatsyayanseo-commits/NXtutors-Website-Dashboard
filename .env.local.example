# Where the Laravel site runs. The dashboard calls its API server-side and
# forwards the visitor's own session cookie, so no second login exists.
LARAVEL_ORIGIN=http://localhost:8000

# Public origin of the site, used when building absolute links back into PHP
# pages the dashboard does not own (pricing, checkout, logout).
NEXT_PUBLIC_SITE_ORIGIN=http://localhost:8000
